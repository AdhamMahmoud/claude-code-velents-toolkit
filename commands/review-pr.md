---
name: review-pr
description: Security-first PR review with severity tiers before merge
user-invocable: true
---

Use the Task tool to invoke `velents-toolkit:pr-reviewer` with:

```
$ARGUMENTS
```

Call the Task tool now with `subagent_type: velents-toolkit:pr-reviewer`. Do not narrate. Just invoke.
