---
name: generate-tests
description: Test generation — auto-generate unit tests, feature tests, integration tests from existing code
user-invocable: true
---

Use the Task tool to invoke `velents-toolkit:test-generator` with:

```
$ARGUMENTS
```

Call the Task tool now with `subagent_type: velents-toolkit:test-generator`. Do not narrate. Just invoke.
