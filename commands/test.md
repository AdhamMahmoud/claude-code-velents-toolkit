---
name: test
description: Testing — run tests, debug failures, test strategy, coverage analysis, CI pipeline
user-invocable: true
---

Use the Task tool to invoke `velents-toolkit:testing-engineer` with:

```
$ARGUMENTS
```

Call the Task tool now with `subagent_type: velents-toolkit:testing-engineer`. Do not narrate. Just invoke.
